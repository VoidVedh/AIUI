# AIUI
Building an AI system that understands visual interfaces, extracts their design language, generates frontend code, and iteratively corrects its output until it closely matches the original design.
